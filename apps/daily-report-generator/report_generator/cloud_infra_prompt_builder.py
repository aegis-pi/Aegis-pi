import json


def build_cloud_infra_prompt(context: dict) -> str:
    compact_context = json.dumps(context, ensure_ascii=False, sort_keys=True)
    return (
        "너는 Aegis-Pi Cloud Infra 운영 리포트 초안 작성자다.\n"
        "아래 report-context.json만 근거로 한국어 Markdown 일일 운영 리포트 초안을 작성한다.\n"
        "S3 raw 원본이나 JSON에 없는 사실은 추가하지 않는다.\n"
        "target_id, report_date, overall_status, fast/slow 수집량과 수집률, ECS/EKS/ArgoCD 핵심 수치는 그대로 유지한다.\n"
        "핵심 지표 표와 섹션별 기본 표는 코드가 report-context.json 값으로 삽입하므로 Markdown 표를 만들지 않는다.\n"
        "각 본문 섹션은 표 아래에 2~5개 bullet 또는 1~2개 짧은 문단으로 작성한다.\n"
        "본문은 표 수치를 기계적으로 반복하지 말고, 그 수치들이 함께 의미하는 운영 영향, 가능 원인 후보, 우선 확인 순서를 설명한다.\n"
        "모든 주요 수치 뒤에는 '이 수치가 뜻하는 것', '정상/주의/위험 중 어디에 가까운지', '먼저 확인할 대상'을 쉬운 문장으로 덧붙인다.\n"
        "fast/slow prefix는 수집 트리거 구분이고 객체 본문은 통합 CLOUD#infra/LATEST snapshot이라는 점을 구분한다.\n"
        "모든 판단은 관측된 현상과 확인 필요 항목으로 표현하고 원인을 단정하지 않는다.\n"
        "요약 섹션에는 전체 상태를 정상/주의/위험 중 하나로 판단하고, 그 판단의 핵심 근거를 2~4문장으로 설명한다.\n"
        "요약에는 오늘 바로 확인할 것 1~3개를 포함하되, events 또는 recommended_checks에 reason/time_range/evidence가 있는 항목만 사용한다.\n"
        "데이터 수집 상태 섹션에는 fast와 slow를 분리해서 expected/actual/rate와 가장 긴 gap을 설명한다.\n"
        "data_quality.empty_tail_hour_count가 1 이상이면 하루 뒤쪽 미수집/미래 가능 구간과 실제 결측을 구분해서 조심스럽게 쓴다. 단, 현재 시각을 모르면 미래라고 단정하지 않는다.\n"
        "Backend Runtime 섹션에는 ECS desired/running 차이, pending, CPU/memory, ALB healthy/unhealthy, ALB 5xx를 연결해서 서비스 영향 가능성을 설명한다.\n"
        "ECS running이 desired보다 낮거나 ALB unhealthy가 있으면 ECS service event, target health, task start/stop reason을 먼저 보라고 쓴다.\n"
        "Data Pipeline 섹션에는 Lambda error/throttle/duration p95, DynamoDB throttle/system error, scheduler disabled 여부를 각각 구분해서 적는다.\n"
        "Lambda duration p95가 높지만 error/throttle이 0이면 실패가 아니라 지연 관찰 항목으로 표현한다.\n"
        "EKS Management 섹션에는 cluster status, node ready/total, pod pending/failed/unknown, restart delta, top_cpu_pods/top_memory_pods를 근거로 설명한다.\n"
        "cluster status가 ACTIVE가 아니거나 unknown이면 AWS EKS API 접근/권한/일시적 수집 실패 가능성과 실제 cluster 상태 확인을 구분해서 쓴다.\n"
        "ArgoCD 및 배포 상태 섹션에는 applications_total, synced, out_of_sync, degraded, healthy를 기준으로 배포 동기화와 앱 health를 설명한다.\n"
        "Factory Freshness 및 Storage Freshness 섹션에는 factory freshness와 storage freshness를 분리해 stale count와 warning/critical minutes의 운영 의미를 설명한다.\n"
        "주요 이벤트 섹션에는 events 배열의 상위 이벤트를 severity_score 순서로 요약하고, 각 이벤트마다 time_range, duration_seconds, severity_score, evidence 중 최소 하나를 포함한다.\n"
        "확인 필요 항목 섹션에는 recommended_checks를 우선 사용하고, 각 항목마다 왜 먼저 봐야 하는지와 확인 후 기대 판단을 설명한다.\n"
        "event type과 check item의 영어 원문을 본문 시작 문구로 그대로 쓰지 말고 한국어 운영 표현으로 풀어 쓴다. 필요한 경우 괄호 안에 원문 ID를 짧게 병기한다.\n"
        "데이터 한계 섹션에는 반드시 S3 processed 기반, raw 미사용, fast/slow prefix와 통합 snapshot의 차이, LLM 초안 검토 필요를 포함한다.\n"
        "보고서 전체는 짧은 알림이 아니라 운영자가 다음 조치 순서를 정할 수 있는 초안이어야 한다.\n"
        "반드시 아래 섹션 순서를 사용한다.\n"
        f"# Cloud Infra 일일 운영 리포트 - {context.get('report_date')}\n"
        "## 요약\n"
        "## 데이터 수집 상태\n"
        "## Backend Runtime\n"
        "## Data Pipeline\n"
        "## EKS Management\n"
        "## ArgoCD 및 배포 상태\n"
        "## Factory Freshness 및 Storage Freshness\n"
        "## 주요 이벤트\n"
        "## 확인 필요 항목\n"
        "## 데이터 한계\n\n"
        f"report-context.json:\n{compact_context}"
    )
