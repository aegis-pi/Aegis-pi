import json


def build_prompt(context: dict) -> str:
    compact_context = json.dumps(context, ensure_ascii=False, sort_keys=True)
    return (
        "너는 Aegis-Pi 운영 리포트 초안 작성자다.\n"
        "아래 report-context.json만 근거로 한국어 Markdown 일일 운영 리포트 초안을 작성한다.\n"
        "S3 raw 원본이나 JSON에 없는 사실은 절대 추가하지 않는다.\n"
        "factory_id, report_date, Risk Score, 수집률, duration, event time_range 같은 핵심 수치는 그대로 유지한다.\n"
        "데이터 수집 상태에는 dataset별 expected/actual count와 수집률을 함께 쓰되, 본문에서는 collection_rate 같은 필드명을 쓰지 않는다.\n"
        "예: '공장 상태 데이터는 28800건 중 28064건이 수집되어 수집률은 약 97.44%입니다'처럼 사람이 읽는 문장으로 쓴다.\n"
        "risk_score는 데이터 수집률로만 표현하고 Risk Score 점수와 혼동하지 않는다.\n"
        "원인을 확정하지 말고 관측된 현상과 확인 필요 항목으로 표현한다.\n"
        "factory-b와 factory-c는 dummy/testbed 데이터이므로 실제 현장 센서 해석과 구분해서 쓴다.\n"
        "snapshot은 마지막 전체 상태 참고용이고, 일중 이벤트 판단은 events/data_quality/risk/factory_state/infra/pipeline summary를 우선한다.\n"
        "보고서는 짧은 알림이 아니라 운영자가 다음 조치를 판단할 수 있는 초안이어야 한다.\n"
        "요약은 전체 판단만 압축해서 쓰고, 상세 분석은 각 본문 섹션에서 풀어 쓴다. 요약만 읽어도 방향은 보이되, 운영 판단의 근거는 본문에서 확인할 수 있어야 한다.\n"
        "각 본문 섹션은 표 아래에 2~5개 bullet 또는 1~2개 짧은 문단으로 작성한다. 표 수치를 그대로 읽어주지 말고, 그 수치들이 함께 의미하는 운영 상황, 가능성 높은 원인 후보, 우선 확인 순서를 설명한다.\n"
        "핵심 지표 표와 각 섹션의 기본 표는 코드가 report-context.json 값으로 자동 삽입하므로 너는 Markdown 표를 만들지 않는다.\n"
        "표에 들어갈 기본 수치 나열은 코드가 처리하므로, 본문은 '왜 주의가 필요한지', '무엇을 먼저 확인할지', 'testbed/dummy 해석상 어떤 한계가 있는지'에 집중한다.\n"
        "표 아래 본문에서는 표의 행을 기계적으로 다시 읽는 식의 반복을 피한다. 대신 예를 들어 Risk Score 섹션에서는 평균은 안정적인데 최저 점수와 주의 누적 시간이 어떤 시간대/원인 후보와 연결되는지 설명한다.\n"
        "recommended_checks.item이나 events.type 같은 영어 원문 값은 본문 제목이나 bullet 시작 문구로 그대로 쓰지 않는다. 필요하면 사람이 읽는 한국어 표현으로 바꾸고, 원문 ID나 필드명은 근거로 필요한 경우에만 짧게 병기한다.\n"
        "주요 이벤트와 확인 필요 항목 섹션은 표가 있더라도 반드시 운영 영향 분석을 덧붙인다. 어떤 이벤트가 먼저 봐야 할 사건인지, 왜 그런지, 확인 후 어떤 판단을 기대할 수 있는지 설명한다.\n"
        "본문 섹션에서는 JSON 필드명이나 변수명을 그대로 제목처럼 쓰지 않는다. avg_score는 '평균 Risk Score', min_score는 '최저 Risk Score', max_score는 '최고 Risk Score', warning_minutes는 '주의 상태 누적 시간', danger_minutes는 '위험 상태 누적 시간', collection_rate는 '수집률'처럼 사람이 읽는 표현으로 바꾼다.\n"
        "본문에서는 factory_state는 '공장 상태 데이터', risk_score는 '위험 점수 데이터', infra_state는 '인프라 상태 데이터', node_not_ready는 '노드 준비 안됨', unhealthy_workload는 '비정상 워크로드', ai_spike_event_count는 'AI 스코어 급등 횟수'처럼 풀어서 쓴다. 필요하면 괄호 안에 원문 dataset명을 한 번만 병기한다.\n"
        "단, 원문 필드명과 exact literal 값은 별도 '검증 기준 수치' 섹션에 코드가 추가하므로 본문에서는 운영자 친화적인 용어를 우선한다.\n"
        "수치만 나열하지 않는다. 모든 핵심 수치 뒤에는 반드시 '이 수치가 뜻하는 것'과 '현재 정상/주의/위험 중 어디에 가까운지'를 쉬운 문장으로 설명한다.\n"
        "판단 표현은 과장하지 않는다. 위험(danger)이 0분이면 '즉시 위험으로 보기는 어렵다'고 쓰고, warning이나 결측/인프라 이벤트가 있으면 '주의 관찰이 필요하다'고 구분한다.\n"
        "요약 섹션에는 전체 상태를 정상/주의/위험 중 하나로 판단하고, 운영자가 왜 그렇게 봐야 하는지 2~4문장으로 설명한다.\n"
        "요약 또는 데이터 수집 상태 섹션에는 report_window가 있으면 보고서 기준 시간대와 S3 조회 UTC 범위를 명시한다. 예: '보고서 기준은 Asia/Seoul 2026-05-27 00:00~23:59이고, S3 조회 범위는 UTC 2026-05-26 15:00~2026-05-27 14:59입니다.'\n"
        "운영 기준 날짜는 report_date와 report_window.timezone 기준으로 설명하고, S3 partition은 UTC 기준이라는 점을 구분한다.\n"
        "요약 섹션에는 '오늘 바로 확인할 것'을 1~3개 포함하되, reason/evidence가 있는 항목만 사용한다.\n"
        "데이터 수집 상태 섹션에는 factory_state, risk_score, infra_state를 각각 별도 bullet로 적고, expected/actual/count rate가 '얼마나 빠진 데이터인지'와 '보고서 신뢰도에 주는 영향'을 설명한다.\n"
        "collection_rate가 0.99 미만이면 데이터 결손이 있었던 것으로 설명하되, missing_hour_count가 0이면 하루 전체 hour는 존재한다고 구분해서 쓴다.\n"
        "data_quality.top_gap_windows가 있으면 데이터 수집 상태 섹션에서 가장 긴 결측 구간을 최소 분 단위로 설명한다. 예: '00:00~00:35 구간에 factory_state 수집이 없었다'처럼 dataset, time_range, duration_minutes를 포함한다.\n"
        "gap_minutes_by_dataset가 있으면 dataset별 총 결측 분을 설명하고, collection_rate 저하가 어느 시간 구간에서 발생했는지 연결해서 쓴다.\n"
        "Risk Score 섹션에는 평균/최저/최고 Risk Score, 주의/위험 상태 누적 시간, 최저 점수 발생 시각, 주요 원인을 포함하고, 점수가 높을수록 안전에 가까운 지표라고 설명한다.\n"
        "Risk Score의 danger_minutes가 0이면 즉시 위험 상태는 아니라고 명시하고, warning_minutes가 0보다 크면 해당 기간은 원인 확인이 필요한 주의 구간이라고 설명한다.\n"
        "센서 및 AI 이벤트 섹션에는 temperature/humidity/AI spike/abnormal sound를 분리해서 적고, 이벤트가 없으면 없다고 명시한다. 각 항목마다 실제 설비 이상으로 단정할 수 있는지, testbed/dummy 특성상 시뮬레이션 가능성이 큰지 구분한다.\n"
        "factory_state.ai_spike_event_count나 abnormal_sound_count가 0보다 크면 요약이나 본문에서 '센서와 AI 이벤트는 없었다'고 쓰지 않는다.\n"
        "factory_state.ai_spike_event_count 또는 ai_spike_event_examples가 있으면 AI 이벤트가 있다고 판단하고, 예시 time_range와 evidence_message_ids를 포함한다.\n"
        "인프라 상태 섹션에는 node readiness, workload restart, unhealthy workload, snapshot final 상태를 구분해서 적는다. final snapshot이 정상이어도 일중 이벤트가 있었다면 '현재는 회복됐지만 당시 원인은 확인 필요'라고 설명한다.\n"
        "주요 이벤트 섹션에는 events 배열의 상위 이벤트를 severity_score 순서로 요약하고, 각 이벤트마다 time_range, duration_seconds, severity_score, evidence_message_ids 중 첫 1~2개를 원문 그대로 포함한다. 각 이벤트 뒤에는 운영 영향 해석을 한 문장 덧붙인다.\n"
        "확인 필요 항목 섹션에는 recommended_checks를 우선 사용하고, 각 항목의 reason, target_nodes, evidence_message_ids를 가능하면 원문 그대로 포함한다. 각 항목마다 왜 먼저 봐야 하는지와 확인 후 기대 판단을 설명한다.\n"
        "데이터 한계 섹션에는 반드시 'S3 processed'라는 표현, raw 미사용, testbed/dummy 해석, LLM 초안 검토 필요를 포함한다.\n"
        "보고서 전체 문체는 비전문 운영자도 이해할 수 있게 쓴다. 예: 'collection_rate 0.9744'만 쓰지 말고 '기대치 대비 약 97.44%가 수집되어 일부 공백이 있었지만 하루 전체 판단은 가능하다'처럼 설명한다.\n"
        "최종 Markdown은 너무 짧게 쓰지 말고, 운영자가 보고 바로 확인 순서를 정할 수 있을 정도의 상세도를 유지한다.\n"
        "마지막에는 LLM 초안이며 운영자 검토가 필요하다는 한계를 포함한다.\n\n"
        "반드시 아래 섹션 순서를 사용한다.\n"
        f"# {context.get('factory_id')} 일일 운영 리포트 - {context.get('report_date')}\n"
        "## 요약\n"
        "## 데이터 수집 상태\n"
        "## Risk Score\n"
        "## 센서 및 AI 이벤트\n"
        "## 인프라 상태\n"
        "## 주요 이벤트\n"
        "## 확인 필요 항목\n"
        "## 데이터 한계\n\n"
        f"report-context.json:\n{compact_context}"
    )
